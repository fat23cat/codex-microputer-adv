name: host-tests

on:
  push:
  pull_request:

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Audit publishable tree
        run: ./tools/audit_public_tree.py
      - name: Run firmware logic regressions
        run: ./tools/test.sh
